// Generated by CoffeeScript 1.12.3
(function() {
  var getOptions, init, markdownIt, markdownItOptions, needsInit;

  markdownIt = null;

  markdownItOptions = null;

  getOptions = function() {
    return {
      html: true,
      xhtmlOut: false,
      breaks: false,
      langPrefix: 'lang-',
      linkify: false,
      typographer: true
    };
  };

  init = function() {
    markdownItOptions = getOptions();
    markdownIt = require('markdown-it')(markdownItOptions);
    return markdownIt;
  };

  needsInit = function(rL) {
    return (markdownIt == null);
  };

  exports.render = function(text) {
    if (needsInit()) {
      init();
    }
    return markdownIt.render(text);
  };

  exports.decode = function(url) {
    return markdownIt.normalizeLinkText(url);
  };

  exports.getTokens = function(text) {
    return markdownIt.parse(text, {});
  };

}).call(this);
